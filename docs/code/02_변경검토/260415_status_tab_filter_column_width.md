# 현황 탭 필터링 모드 열 너비 조정 (충방전기·Step/Cycle/총Cycle)

> **작성일**: 2026-04-15
> **대상 파일**: `DataTool_dev_code/DataTool_optRCD_proto_.py`
> **변경 위치**: `filter_all_channels()` 내부 `_fixed_widths` 딕셔너리 (line 26794-26803)

---

## 배경

현황 테이블 필터링 모드에서 다음 두 열이 빡빡하게 느껴진다는 피드백:

- **충방전기** (col 0): 48px — "PNE24" 5자 기준이라 "PNE101" 같이 3자리 번호 케이스에서 여유 부족
- **Step/Cycle/총Cycle** (col 4): 92px — 데이터("000/000/0000", 12자)는 맞지만 **헤더 문자열**(18자)이 잘림

## 변경 내용

| 열 | 컬럼명 | Before | After | Δ |
|---|---|---|---|---|
| 0 | 충방전기 | 48 | **58** | +10 (+21%) |
| 4 | Step/Cycle/총Cycle | 92 | **112** | +20 (+22%) |

그 외 열(채널/상태/경과/전압/동작/온도/테스트명/셀경로) 너비·모드 변화 없음.

## 기대 효과

- "PNE101" 같은 3자리 번호도 잘림 없이 표시
- Step/Cycle 헤더 가독성 개선 (완전 노출은 아니지만 잘림 완화)
- 고정폭 열 2개만 건드리므로 Stretch 열(테스트명/셀경로) 잔여 공간 소폭 축소

## 회귀 영향

- 일반 모드(초기 행/열 설정)는 변화 없음
- 행 높이, 섹션 토글, 컬럼 개수/순서 변화 없음
- 컬럼 `ResizeMode` 변경 없음 (col 0·4 모두 `Interactive` 유지 → 사용자가 수동 리사이즈 가능)
