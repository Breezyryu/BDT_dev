# 현황 탭 필터링 열 너비 추가 확장 (충방전기·Step/Cycle/총Cycle +20%)

> **작성일**: 2026-04-15
> **대상 파일**: `DataTool_dev_code/DataTool_optRCD_proto_.py`
> **변경 위치**: `filter_all_channels()` 내부 `_fixed_widths` (line 26794-26803)

---

## 배경

같은 날 1차 확장(`260415_status_tab_filter_column_width.md`) 후에도 충방전기/Step·Cycle 열이 좀 더 넓어졌으면 한다는 요청. 직전 값에서 **20% 증가** 적용.

## 변경 내용

| 열 | 컬럼명 | Before | After | Δ |
|---|---|---|---|---|
| 0 | 충방전기 | 58 | **70** | +12 (+20.7%) |
| 4 | Step/Cycle/총Cycle | 112 | **134** | +22 (+19.6%) |

그 외 열·모드·매칭 로직 변화 없음.

## 효과

- 충방전기 헤더("충방전기") 와 데이터(예: "PNE101") 모두 여유 표시
- Step/Cycle/총Cycle 헤더 잘림 추가 완화 ("000/000/0000" 데이터 + 헤더 동시 가독)
- Stretch 열(테스트명/셀경로) 잔여 공간은 약 34px 추가 축소 — 화면 폭 좁을 경우 모니터링 권장

## 회귀 영향

- 일반 모드(초기 행/열 설정)는 변화 없음
- 행 높이, 섹션 토글, 컬럼 개수/순서 변화 없음
- 사용자 수동 리사이즈(`Interactive` 모드)는 그대로 가능
