# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
Rawdata/
Parameter_Generator_v9.4_for_MX_2026/
DataTool/
BAK/
raw/
# vault/ — 2026-04-21: vault was merged into wiki/ (tracked as LLM-owned knowledge)
*.egg-info
*.exe
*.spec
# Virtual environments
.venv
.agent
# Secrets / tokens — NEVER commit
.env
.env.*
!.env.example
*.token
secrets/
.vscode/
!tools/doc_converter/.vscode/
!tools/doc_converter/.vscode/**
.github/
analysis_dev_env/
.gitignore
data/
.claude/
CLAUDE.md
# Added by code-review-graph
.code-review-graph/
# Vercel-labs `skills` CLI lockfile — body(.claude/skills/) is already ignored, lockfile has no value alone
skills-lock.json
# Phase 0 audit debug output — large, regenerable from sch_dump.py
tools/sch_dump_output.txt
# PyInstaller build artifacts — regenerable
DataTool_dev_build/
# Separate project — should be its own repo
bdt_data_platform/
# Obsidian editor state / plugins
wiki/.obsidian/
# Wiki generated outputs
wiki/outputs/
# humanize-korean 작업 산출물 (run_id 폴더)
_workspace/
tests/regression/baselines/
